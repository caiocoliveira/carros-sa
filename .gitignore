# Credenciais — NUNCA commitar
.env
.env.*
!.env.example
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
.venv.*/
.pytest_cache/
*.egg-info/
dist/
build/
# uv lockfile — gerado on-demand pelo `uv pip install`. Não versionamos
# porque o ambiente reproduzível é o pyproject.toml + `uv venv --python 3.12`
# (ver CLAUDE.md/Setup); versionar uv.lock fixaria hashes do ambiente local
# da sessão (potencialmente Python 3.11 vs 3.12), poluindo o repo.
uv.lock

# Dados locais (PoC)
*.db
*.db-journal
data/scrapes/*.json
data/laudos_amostra/*.pdf
data/cache/
# PDFs baixados pelo scraper em runtime (dir criado por _pdf_persistente_path).
# Não versionados — são cache local grande (3-14MB por PDF × centenas de lotes).
data/laudos_pdfs/
# Exceções: dataset de referência — usado como fixture pelos testes/workstreams
!data/scrapes/2026-04-14_uberlandia_listagem.json
!data/scrapes/2026-04-14_webmotors_fiesta.json
!data/laudos_amostra/21854782_fiesta.pdf

# OS / editor
.DS_Store
.vscode/
.idea/

# Claude Code local state
.claude/settings.local.json
